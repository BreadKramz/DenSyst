<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}DenSyst{% endblock %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script>
      // Simple toggle for mobile menu
      function toggleMenu() {
        document.getElementById('mobileMenu').classList.toggle('hidden');
      }
    </script>
</head>
<body class="bg-gray-50 text-gray-800 font-sans">

    <nav class="bg-[#1A4487] text-white shadow-lg">
        <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
            <div class="flex items-center space-x-3">
                <img src="{{ asset('images/densyst-logo.png') }}" alt="DenSyst Logo" class="h-10 w-auto">
                <span class="text-2xl font-extrabold tracking-wide">DenSyst</span>
            </div>

            <ul class="hidden md:flex space-x-8 font-medium">
                <li><a href="{{ path('home') }}" class="hover:text-[#3D7A3F] transition">Home</a></li>
                <li><a href="{{ path('features') }}" class="hover:text-[#3D7A3F] transition">Features</a></li>
                <li><a href="{{ path('about') }}" class="hover:text-[#3D7A3F] transition">About</a></li>
                <li><a href="{{ path('contact') }}" class="hover:text-[#3D7A3F] transition">Contact</a></li>
            </ul>

            <a href="{{ path('login') }}" class="hidden md:inline bg-[#3D7A3F] px-5 py-2 rounded-lg font-semibold hover:bg-green-700 transition">
                Login
            </a>

            <button onclick="toggleMenu()" class="md:hidden focus:outline-none">
                ☰
            </button>
        </div>

        <div id="mobileMenu" class="hidden md:hidden px-6 pb-4 space-y-2 bg-[#132d60]">
            <a href="{{ path('home') }}" class="block hover:text-[#3D7A3F]">Home</a>
            <a href="{{ path('features') }}" class="block hover:text-[#3D7A3F]">Features</a>
            <a href="{{ path('about') }}" class="block hover:text-[#3D7A3F]">About</a>
            <a href="{{ path('contact') }}" class="block hover:text-[#3D7A3F]">Contact</a>
            <a href="{{ path('login') }}" class="block bg-[#3D7A3F] px-4 py-2 rounded-lg text-center hover:bg-green-700">Login</a>
        </div>
    </nav>

    <main>
        {% block body %}{% endblock %}
    </main>

    <footer class="bg-[#132d60] text-gray-200">
        <div class="max-w-7xl mx-auto px-6 py-10 grid md:grid-cols-3 gap-8">
            
            <!-- Brand -->
            <div>
                <h2 class="text-2xl font-bold text-white">DenSyst 🦷</h2>
                <p class="mt-3 text-sm text-gray-400">
                    Your trusted dental management system for smarter scheduling, 
                    patient care, and clinic insights.
                </p>
            </div>

            <!-- Quick Links -->
            <div>
                <h3 class="text-lg font-semibold text-white mb-3">Quick Links</h3>
                <ul class="space-y-2">
                    <li><a href="{{ path('home') }}" class="hover:text-[#3D7A3F] transition">Home</a></li>
                    <li><a href="{{ path('features') }}" class="hover:text-[#3D7A3F] transition">Features</a></li>
                    <li><a href="{{ path('about') }}" class="hover:text-[#3D7A3F] transition">About</a></li>
                    <li><a href="{{ path('contact') }}" class="hover:text-[#3D7A3F] transition">Contact</a></li>
                </ul>
            </div>

            <!-- Contact Info -->
            <div>
                <h3 class="text-lg font-semibold text-white mb-3">Contact</h3>
                <p class="text-sm text-gray-400">📍 Dumaguete City, Philippines</p>
                <p class="text-sm text-gray-400">📧 breadkramz@gmail.com</p>
                <p class="text-sm text-gray-400">📞 +63 945 605 7652</p>
            </div>
        </div>

        <!-- Bottom Bar -->
        <div class="border-t border-gray-700 py-4 text-center text-sm text-gray-400">
            &copy; 2025 <span class="text-white font-semibold">DenSyst</span>. All rights reserved.
            <span class="text-white font-semibold">BreadKramz.</span>
        </div>
    </footer>

</body>
</html>
