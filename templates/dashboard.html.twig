<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Dashboard - DenSyst</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-50 min-h-screen">
    <nav class="bg-[#1A4487] text-white p-4 flex justify-between items-center shadow-md">
        <span class="font-semibold">Welcome, {{ user }}</span>
        <a href="{{ path('logout') }}" class="bg-red-600 px-4 py-2 rounded-lg hover:bg-red-800 transition">Logout</a>
    </nav>

    <main class="p-10 max-w-6xl mx-auto">
        <h1 class="text-4xl font-bold mb-10">Dashboard</h1>
        <div class="grid md:grid-cols-3 gap-8">
            <div class="bg-white p-8 rounded-lg shadow hover:shadow-lg transition text-center">
                <h2 class="text-xl font-semibold mb-2">📅 Appointments</h2>
                <p class="text-gray-600">View and manage all bookings.</p>
            </div>
            <div class="bg-white p-8 rounded-lg shadow hover:shadow-lg transition text-center">
                <h2 class="text-xl font-semibold mb-2">🦷 Patients</h2>
                <p class="text-gray-600">Access patient records and history.</p>
            </div>
            <div class="bg-white p-8 rounded-lg shadow hover:shadow-lg transition text-center">
                <h2 class="text-xl font-semibold mb-2">📊 Reports</h2>
                <p class="text-gray-600">Generate insights on clinic performance.</p>
            </div>
        </div>
    </main>
</body>
</html>
